# memory: jdbc:h2:mem:productDB
# store data: jdbc:h2:file:C://Data
spring.datasource.url=jdbc:h2:mem:blogDB
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=developer
spring.datasource.password=BlogApp@2021
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
# in case using store data with file
# only first time run app: create-drop
# spring.jpa.hibernate.ddl-auto=create-drop
#spring.datasource.initialization-mode=always
# allow to open console
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console
# prevent remote connection
spring.h2.console.settings.web-allow-others=false